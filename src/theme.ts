import { createTheme } from "@mui/material";

const theme = createTheme({
  typography: {
    fontFamily: '"Inter", sans-serif', // Nota: incluye comillas si el nombre contiene espacios
  },
});

export default theme;
